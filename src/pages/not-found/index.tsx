import React, { FC } from "react";
import { PageWrapper } from "src/app/layout/PageWrapper";

const NotFound: FC = () => (
  <PageWrapper>
    <section className="container">NotFound</section>
  </PageWrapper>
);

export default NotFound;
